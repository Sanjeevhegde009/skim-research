# Python
__pycache__/
*.pyc
venv/
.venv/

# Datasets — run ./download_data.sh (large; not committed)
/data/*
!/data/.gitkeep

# Regenerated caches (index ToC, embeddings, LongMemEval summaries/indexes)
/cache/

# Eval outputs (regenerated; numbers are documented in README instead)
/results/*
!/results/.gitkeep

# Secrets — keys live in env vars only, never in files
.env
*.key
